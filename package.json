{
  "name": "mini-antui",
  "version": "1.0.0",
  "description": "小程序版AntUI",
  "repository": {
    "type": "git",
    "url": "git@gitlab.alipay-inc.com:mini-program/mini-antui.git"
  },
  "keywords": [
    "antui",
    "mini-program"
  ],
  "devDependencies": {
    "rc-tools": "6.x"
  },
  "scripts": {
    "build": "rc-tools run compile && node scripts/cp.js",
    "pub": "git push origin && npm run build && tnpm publish"
  }
}
